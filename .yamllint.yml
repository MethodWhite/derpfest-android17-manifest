---
extends: default
yaml-files:
  - '*.yaml'
  - '*.yml'
rules:
  document-start: disable
  line-length:
    max: 120
  truthy:
    allowed-values: ['true', 'false']
  comments-indentation: enable
  empty-lines:
    max: 2
